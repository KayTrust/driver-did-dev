apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{TAG_ENV}}kt-provider-resolver-did-ev-deployment"
  namespace: {{NAMESPACE}}
  labels:
    app: {{TAG_ENV}}kt-provider-resolver-did-ev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{TAG_ENV}}kt-provider-resolver-did-ev
  strategy:
    type: Recreate      
  template:
    metadata:
      labels:
        app: {{TAG_ENV}}kt-provider-resolver-did-ev
    spec:
      containers:
        - image: {{DOCKER_IMAGE_TAG}}
          name: {{TAG_ENV}}kt-provider-resolver-did-ev
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: NODE_HOST
              value: "http://11.0.1.4"
            - name: ADDRESS_IM
              value: "0xa6b4540a2bfbe8663caa78027c83d0dcb1b7c837"
      imagePullSecrets:
        - name: provider-docker-registry
      restartPolicy: Always
      
---
apiVersion: v1
kind: Service
metadata:
  name: "{{TAG_ENV}}kt-provider-resolver-did-ev-service"
  namespace: {{NAMESPACE}}
  labels:
    app: {{TAG_ENV}}kt-provider-resolver-did-ev
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
  selector:
    app: {{TAG_ENV}}kt-provider-resolver-did-ev
  type: ClusterIP